# Setup

    cd nextbus
    npm install

That's it! To run it:

    node nextbus.js "BUS ROUTE" "BUS STOP NAME" "DIRECTION"

# Testing

To run the test suite, use either of these:

    mocha
    npm test
